<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Ticket d'achat</title>
    <style>
      body {
    font-family: Arial, sans-serif;
    font-size: 12px;
    color: red;
    background-repeat: no-repeat;
    background-size: cover;
}

        h1 {
            text-align: center;
            font-size: 20px;
        }
        table {
            border-collapse: collapse;
            width: 100%;
            margin-top: 20px;
        }
        th, td {
            border: 1px solid #ddd;
            text-align: left;
            padding: 8px;
        }
        th {
            background-color: #f2f2f2;
        }
    </style>
</head>
<body>
    <h1>Ticket d'achat</h1>
    <p>Date: {{ date|date('d/m/Y H:i:s') }}</p>
    <table>
        <thead>
            <tr>
                <th>Nom</th>
                <th>Prix unitaire</th>
                <th>Quantité</th>
                <th>Prix total</th>
            </tr>
        </thead>
        <tbody>
            {% set total = 0 %}
            {% for p in produits %}
                {% set prix_total = p.IdProduct.getPrix * p.getQuantite %}
                {% set total = total + prix_total %}
                <tr>
                    <td>{{ p.IdProduct.getLib }}</td>
                    <td>{{ p.IdProduct.getPrix }} €</td>
                    <td>{{ p.getQuantite }}</td>
                    <td>{{ p.IdProduct.getPrix *  p.getQuantite  }} €</td>
                </tr>
            {% endfor %}
        </tbody>
        <tfoot>
            <tr>
                <td colspan="3" style="text-align: right;"><strong>Total :</strong></td>
                <td><strong>{{ total }} €</strong></td>
            </tr>
        </tfoot>
    </table>
</body>
</html>